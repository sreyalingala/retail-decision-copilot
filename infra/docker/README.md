# Local Development (Docker)

This compose file is a placeholder for local wiring:
- Postgres (development)
- Backend (FastAPI Docker)
- Frontend (Next.js)

Next step:
- Add `apps/api` and `apps/web` services and connect them to `postgres`.

