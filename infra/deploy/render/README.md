# Render Deployment (Backend)

Placeholder deployment notes for the FastAPI backend running in Docker.

Next steps:
- Create Render service using the backend `apps/api/Dockerfile`
- Configure environment variables:
  - `OPENAI_API_KEY`
  - `DATABASE_URL` (Neon/Postgres)
  - `DATABASE_READONLY_URL`
  - `OPENAI_MODEL`, `SQL_MAX_ROWS`, etc.

