# Shared types (@rdc/shared)

Shared TypeScript contracts between the Next.js frontend and FastAPI backend.

Next steps:
- Define request/response shapes for `POST /query`
- Share any structured “assistant turn” schema with the UI

